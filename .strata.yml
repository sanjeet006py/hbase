global:
  email-reply-to: bigdata-hbase-team@salesforce.com # Change this to your group's e-mail address!
  email-only-last-committer-on-dev-branch: true
  timeout: 300
  production-branches: 
    - 2.5.10-sfdc-13
stages:
  integration-test:
    - maven-verify:
        maven-publish-jar-image: docker.repo.local.sfdc.net/sfci/docker-images/sfdc_centos7_jdk8
        maven-publish-jar-args: -e -Pjacoco -DskipITs -Dmaven.test.failure.ignore=true -Dsurefire.testFailureIgnore=true
